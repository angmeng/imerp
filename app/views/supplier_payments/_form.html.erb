<%= simple_form_for(@supplier_payment) do |f| %>
  <% if @supplier_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supplier_payment.errors.count, "error") %> prohibited this supplier_payment from being saved:</h2>

      <ul>
      <% @supplier_payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
    <%= f.input :supplier_payment_number %>
    <%= f.input :supplier_payment_date %>
    <%= f.input :remark %>
    <%= f.input :voided %>
    <%= f.input :supplier_id %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
