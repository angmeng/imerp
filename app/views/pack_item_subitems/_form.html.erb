<%= simple_form_for(@pack_item_subitem) do |f| %>
  <% if @pack_item_subitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pack_item_subitem.errors.count, "error") %> prohibited this pack_item_subitem from being saved:</h2>

      <ul>
      <% @pack_item_subitem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
    <%= f.input :pack_item_id %>
    <%= f.input :product_id %>
    <%= f.input :product_uom_id %>
    <%= f.input :stock_location_id %>
    <%= f.input :quantity %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
